import rps
from random import choice
a = ["paper", "rock", "scissors"]
# User enters the choice
# Computer picks from those choices, using choice(a)
# Run rps function, determine who wins. Pass in rps parameters as shown earlier.
#Add play again function(if wanted)
